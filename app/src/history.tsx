// 画面遷移用のファイル
// browserHistory.push('path')で遷移可能
// https://coders-shelf.com/react-router-push-in-action/
import { createBrowserHistory } from "history";
export const browserHistory = createBrowserHistory()